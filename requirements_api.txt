fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
pydantic>=2.0.0
python-dotenv>=1.0.0

# Inference. onnxruntime is ~50 MB; torch is NOT needed at runtime — it is only
# used by export_onnx.py to regenerate the .onnx from best_CustomCNN.pth.
onnxruntime>=1.17.0
numpy>=1.26.0
Pillow>=10.0.0
